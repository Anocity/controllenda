╔═══════════════════════════════════════════════════════════════╗
║          🚀 GUIA RÁPIDO - DEPLOY NO VERCEL                    ║
╚═══════════════════════════════════════════════════════════════╝


┌───────────────────────────────────────────────────────────────┐
│  OPÇÃO 1: VERCEL COM GITHUB (5 MINUTOS)                      │
└───────────────────────────────────────────────────────────────┘

1️⃣  FAZER PUSH PARA GITHUB

   cd /app
   git add .
   git commit -m "MIR4 Manager - versão standalone"
   git push


2️⃣  ACESSAR VERCEL

   → https://vercel.com
   → Click "Sign Up" ou "Login"
   → Use sua conta GitHub


3️⃣  IMPORTAR PROJETO

   → Click "Add New..." → "Project"
   → Click "Import" no seu repositório
   → Configure:

   ┌─────────────────────────────────────────┐
   │ Framework Preset:  Create React App    │
   │ Root Directory:    frontend            │
   │ Build Command:     yarn build          │
   │ Output Directory:  build               │
   └─────────────────────────────────────────┘


4️⃣  DEPLOY

   → Click "Deploy"
   → Aguarde 2 minutos


5️⃣  PRONTO! ✅

   Seu site: https://mir4-manager-xxxxx.vercel.app


┌───────────────────────────────────────────────────────────────┐
│  OPÇÃO 2: VERCEL CLI (3 MINUTOS)                             │
└───────────────────────────────────────────────────────────────┘

1️⃣  INSTALAR CLI

   npm install -g vercel


2️⃣  FAZER DEPLOY

   cd /app
   vercel

   → Login com GitHub
   → Aceitar padrões
   → Done!


┌───────────────────────────────────────────────────────────────┐
│  ESTRUTURA SIMPLES                                            │
└───────────────────────────────────────────────────────────────┘

/app/
├── frontend/          ← TODO O CÓDIGO AQUI
│   ├── src/
│   │   ├── pages/Dashboard.js      (usa localStorage)
│   │   └── components/
│   └── package.json
├── vercel.json        ← Configuração Vercel
└── README.md          ← Documentação


┌───────────────────────────────────────────────────────────────┐
│  O QUE VOCÊ TEM AGORA                                         │
└───────────────────────────────────────────────────────────────┘

✅ Site 100% frontend
✅ Sem backend para configurar
✅ Sem banco de dados para configurar
✅ Sem variáveis de ambiente
✅ Dados salvos no navegador (localStorage)
✅ Deploy em 1 clique
✅ Grátis para sempre


┌───────────────────────────────────────────────────────────────┐
│  CARACTERÍSTICAS TÉCNICAS                                     │
└───────────────────────────────────────────────────────────────┘

Linguagem:         JavaScript/React
Framework:         Create React App
Estilos:           Tailwind CSS
Componentes:       Shadcn UI
Armazenamento:     localStorage (navegador)
Build:             ~111 KB (gzipped)
Compatibilidade:   Todos navegadores modernos


┌───────────────────────────────────────────────────────────────┐
│  CONFIGURAÇÃO NO VERCEL                                       │
└───────────────────────────────────────────────────────────────┘

IMPORTANTE: Use estas configurações exatas!

┌──────────────────────────────────────────────────┐
│ Framework Preset      Create React App          │
│ Root Directory        frontend                  │
│ Build Command         yarn build                │
│ Output Directory      build                     │
│ Install Command       yarn install              │
│ Development Command   yarn start                │
└──────────────────────────────────────────────────┘

Environment Variables: NENHUMA! ✅


┌───────────────────────────────────────────────────────────────┐
│  VERIFICAR SE ESTÁ FUNCIONANDO                                │
└───────────────────────────────────────────────────────────────┘

Local:
→ cd frontend
→ yarn start
→ http://localhost:3000

Produção:
→ https://seu-app.vercel.app
→ Click "Nova Conta"
→ Preencha dados
→ Se salvar = Funcionou! ✅


┌───────────────────────────────────────────────────────────────┐
│  TROUBLESHOOTING                                              │
└───────────────────────────────────────────────────────────────┘

❌ "Build Failed"
   → Verifique Root Directory: frontend
   → Verifique se package.json existe em /frontend

❌ "Site em branco"
   → F12 → Console → Ver erros
   → Limpar cache do navegador
   → Testar em modo anônimo

❌ "Dados não salvam"
   → Verifique se cookies estão habilitados
   → Não use modo anônimo para testes

❌ "yarn build falha localmente"
   → cd frontend
   → rm -rf node_modules
   → yarn install
   → yarn build


┌───────────────────────────────────────────────────────────────┐
│  DEPOIS DO DEPLOY                                             │
└───────────────────────────────────────────────────────────────┘

✅ Compartilhe o link com outros
✅ Dados de cada usuário ficam no navegador dele
✅ Cada pessoa tem seus próprios dados
✅ Sem limite de usuários
✅ Sem custo adicional


┌───────────────────────────────────────────────────────────────┐
│  FAZER BACKUP DOS DADOS                                       │
└───────────────────────────────────────────────────────────────┘

No navegador, pressione F12 e cole:

// EXPORTAR
const backup = {
  accounts: localStorage.getItem('mir4_accounts'),
  prices: localStorage.getItem('mir4_boss_prices')
};
console.log(JSON.stringify(backup));
// Copie o texto e salve em arquivo .txt

// IMPORTAR
const backup = /* cole aqui o backup */;
localStorage.setItem('mir4_accounts', backup.accounts);
localStorage.setItem('mir4_boss_prices', backup.prices);
location.reload();


┌───────────────────────────────────────────────────────────────┐
│  COMANDOS ÚTEIS                                               │
└───────────────────────────────────────────────────────────────┘

# Testar localmente
cd frontend && yarn start

# Build local
cd frontend && yarn build

# Deploy no Vercel
vercel

# Atualizar site (push = redeploy automático)
git add .
git commit -m "Atualização"
git push


╔═══════════════════════════════════════════════════════════════╗
║                   ✨ RESUMO FINAL                             ║
╚═══════════════════════════════════════════════════════════════╝

Você tem:
✅ Projeto limpo e organizado
✅ Sem backend ou banco de dados
✅ Pronto para deploy no Vercel
✅ 100% funcional

Para colocar online:
1. git push (GitHub)
2. Import no Vercel
3. Root Directory: frontend
4. Deploy

Tempo total: ~5 minutos
Custo: GRÁTIS

Seu site estará em: https://seu-app.vercel.app

🎉 Pronto para usar!
